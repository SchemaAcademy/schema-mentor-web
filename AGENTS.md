# SchemaMentor 项目约定

这是一个中文存储引擎交互教学平台。后续 AI 新增或修改页面时，先阅读 [设计与交互原则](docs/design-principles.md)。用户当前指令优先。

- 从具体学习问题出发，形成“预测 → 操作 → 观察 → 解释 → 验证”的闭环。
- 沿用 `app/globals.css` 的 token、SiteNav、Icon、LabCatalog 与已有实验布局。
- 领域算法放 `lib/`，不可变教学帧与展示分离；图形、文字和指标由同一个当前帧驱动。
- 动画展示因果，支持暂停、单步、重播和减少动画；节点使用稳定 ID。
- 清楚标记模型边界；不要虚构磁盘 I/O、性能、学习进度或未实现功能。
- 未完成的实验显示“筹备中”，不得链接到不存在的路由。
- 保持静态导出与 BASE_PATH 支持；默认无需服务端、账号或远程字体。
- 为非平凡算法/状态变化添加有意义的测试；完成后运行 `npm run lint`、`npm run test`、`npm run build`。
- 涉及页面交互时实际打开本地浏览器验证主要流程和响应式布局。

开发：`npm ci`，`npm run dev`（http://127.0.0.1:3000）。
